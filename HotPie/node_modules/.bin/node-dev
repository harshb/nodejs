#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../node-dev/node-dev" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../node-dev/node-dev" "$@"
  ret=$?
fi
exit $ret
